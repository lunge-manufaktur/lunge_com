---
layout: false
directory_index: false
---
- pages = sitemap.resources.find_all{|p| p.source_file.match(/\.html/) && !p.data.sitemap_noindex == true }
doctype xml
urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9"
  - pages.each do |p|
    url
      loc = "https://www.lunge.com#{p.url.gsub(/\/+$/,'')}"
      lastmod    = Date.today.to_time.iso8601
      changefreq = p.data.sitemap_changefreq || 'weekly'
      priority   = p.data.sitemap_priority   || '0.5'