<% data.products.classic_walk.colors.each do |color| %>
  $('[data-sku="<%= color.sku %>"]').on("click", function() {
    $(".color-icon").removeClass("color-icon--active");
    $(this).addClass("color-icon--active");
    $('#color-preview').attr("src", "<%= color.default_image %>");
    $('#color-preview__label').html("<%= color.name %>")
  });
<% end %>

$('[data-product="classic_run"] li.color-icon').on("mouseover tap", function(event) {
  event.preventDefault();
  var sku = $(this).data("color");
  var imagePath = $(this).data("image-path")
  $('[data-product=classic_run] .card__image').attr("src", imagePath);
});

$('[data-product="classic_walk"] li.color-icon').on("mouseover tap", function(event) {
  event.preventDefault();
  var sku = $(this).data("color");
  var imagePath = $(this).data("image-path")
  $('[data-product=classic_walk] .card__image').attr("src", imagePath);
});

$('[data-product="neo_run"] li.color-icon').on("mouseover tap", function(event) {
  event.preventDefault();
  var sku = $(this).data("color");
  var imagePath = $(this).data("image-path")
  $('[data-product=neo_run] .card__image').attr("src", imagePath);
});

$('[data-product="neo_walk"] li.color-icon').on("mouseover tap", function(event) {
  event.preventDefault();
  var sku = $(this).data("color");
  var imagePath = $(this).data("image-path")
  $('[data-product=neo_walk] .card__image').attr("src", imagePath);
});

$('[data-product="integer_walk_cushion"] li.color-icon').on("mouseover tap", function(event) {
  event.preventDefault();
  var sku = $(this).data("color");
  var imagePath = $(this).data("image-path")
  $('[data-product=integer_walk_cushion] .card__image').attr("src", imagePath);
});

$('[data-product="feetality"] li.color-icon').on("mouseover tap", function(event) {
  event.preventDefault();
  var sku = $(this).data("color");
  var imagePath = $(this).data("image-path")
  $('[data-product=feetality] .card__image').attr("src", imagePath);
});

$('[data-product="integer_walk_cushion"] li.color-icon').on("mouseover tap", function(event) {
  event.preventDefault();
  var sku = $(this).data("color");
  var imagePath = $(this).data("image-path")
  $('[data-product=integer_walk_cushion] .card__image').attr("src", imagePath);
});

$('[data-product="integer_run_rebound"] li.color-icon').on("mouseover tap", function(event) {
  event.preventDefault();
  var sku = $(this).data("color");
  var imagePath = $(this).data("image-path")
  console.log(imagePath);
  $('[data-product=integer_run_rebound] .card__image').attr("src", imagePath);
});

$('[data-product="integer_walk_rebound"] li.color-icon').on("mouseover tap", function(event) {
  event.preventDefault();
  var sku = $(this).data("color");
  var imagePath = $(this).data("image-path")
  $('[data-product=integer_walk_rebound] .card__image').attr("src", imagePath);
});