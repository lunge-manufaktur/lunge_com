// POLYFILLS
// array.prototype.find()
if (!Array.prototype.find) {
  Object.defineProperty(Array.prototype, 'find', {
    value: function(predicate) {
     // 1. Let O be ? ToObject(this value).
      if (this == null) {
        throw new TypeError('"this" is null or not defined');
      }

      var o = Object(this);

      // 2. Let len be ? ToLength(? Get(O, "length")).
      var len = o.length >>> 0;

      // 3. If IsCallable(predicate) is false, throw a TypeError exception.
      if (typeof predicate !== 'function') {
        throw new TypeError('predicate must be a function');
      }

      // 4. If thisArg was supplied, let T be thisArg; else let T be undefined.
      var thisArg = arguments[1];

      // 5. Let k be 0.
      var k = 0;

      // 6. Repeat, while k < len
      while (k < len) {
        // a. Let Pk be ! ToString(k).
        // b. Let kValue be ? Get(O, Pk).
        // c. Let testResult be ToBoolean(? Call(predicate, T, « kValue, k, O »)).
        // d. If testResult is true, return kValue.
        var kValue = o[k];
        if (predicate.call(thisArg, kValue, k, o)) {
          return kValue;
        }
        // e. Increase k by 1.
        k++;
      }

      // 7. Return undefined.
      return undefined;
    },
    configurable: true,
    writable: true
  });
}



// OFFCANVAS-NAV
$('#offcanvas-menu__toggle').on('click', function(event) {
  event.preventDefault();
  $('body').toggleClass('offcanvas');
})

$('body').on('click', '.overlay', function(event) {
  event.preventDefault();
  $('body').toggleClass('offcanvas');
})



// TABS
$('.tab-title').on('click', function(e) {
  e.preventDefault();

  console.log("tab triggered");

  var target = $(this).children('a').attr('href');
  
  // add active class to tab titles
  $('.tab-title').removeClass("tab-title--active");
  $(this).addClass("tab-title--active");

  // switch to correct tab
  $('.tab').removeClass("tab--active");
  $('.tab[data-tab="' + target +'"]').addClass("tab--active");
});



// CART
$("[data-action='toggle-cart']").on("click", function() {
  $("[data-embed_type='cart_content']").toggleClass("js-active");
});



// PRODUCT-VARIANT PREVIEWS
<% data.products.classic_walk.colors.each do |color| %>
  $('[data-sku="<%= color.sku %>"]').on("click", function() {
    $(".color-icon").removeClass("color-icon--active");
    $(this).addClass("color-icon--active");
    $('#color-preview').attr("src", "<%= color.default_image %>");
    $('#color-preview__label').html("<%= color.name %>")
  });
<% end %>

$('[data-product="classic_run"] .color-icon').on("mouseover tap", function(event) {
  event.preventDefault();
  var sku = $(this).data("color");
  var imagePathPNG = $(this).data("image-path-png");
  var imagePathWEBP = $(this).data("image-path-webp");

  $('[data-product=classic_run] .card__image-container [type="image/png"]').attr("srcset", imagePathPNG);
  $('[data-product=classic_run] .card__image-container [type="image/webp"]').attr("srcset", imagePathWEBP);
  $('[data-product=classic_run] .card__image').attr("src", imagePathPNG);
});

$('[data-product="classic_walk"] .color-icon').on("mouseover tap", function(event) {
  event.preventDefault();
  var sku = $(this).data("color");
  var imagePathPNG = $(this).data("image-path-png");
  var imagePathWEBP = $(this).data("image-path-webp");

  $('[data-product=classic_walk] .card__image-container [type="image/png"]').attr("srcset", imagePathPNG);
  $('[data-product=classic_walk] .card__image-container [type="image/webp"]').attr("srcset", imagePathWEBP);
  $('[data-product=classic_walk] .card__image').attr("src", imagePathPNG);
});

$('[data-product="classic_walk_n"] .color-icon').on("mouseover tap", function(event) {
  event.preventDefault();
  var sku = $(this).data("color");
  var imagePathPNG = $(this).data("image-path-png");
  var imagePathWEBP = $(this).data("image-path-webp");

  $('[data-product=classic_walk_n] .card__image-container [type="image/png"]').attr("srcset", imagePathPNG);
  $('[data-product=classic_walk_n] .card__image-container [type="image/webp"]').attr("srcset", imagePathWEBP);
  $('[data-product=classic_walk_n] .card__image').attr("src", imagePathPNG);
});

$('[data-product="classic_run_n"] .color-icon').on("mouseover tap", function(event) {
  event.preventDefault();
  var sku = $(this).data("color");
  var imagePathPNG = $(this).data("image-path-png");
  var imagePathWEBP = $(this).data("image-path-webp");

  $('[data-product=classic_run_n] .card__image-container [type="image/png"]').attr("srcset", imagePathPNG);
  $('[data-product=classic_run_n] .card__image-container [type="image/webp"]').attr("srcset", imagePathWEBP);
  $('[data-product=classic_run_n] .card__image').attr("src", imagePathPNG);
});

$('[data-product="classic_walk_r"] .color-icon').on("mouseover tap", function(event) {
  event.preventDefault();
  var sku = $(this).data("color");
  var imagePathPNG = $(this).data("image-path-png");
  var imagePathWEBP = $(this).data("image-path-webp");

  $('[data-product=classic_walk_r] .card__image-container [type="image/png"]').attr("srcset", imagePathPNG);
  $('[data-product=classic_walk_r] .card__image-container [type="image/webp"]').attr("srcset", imagePathWEBP);
  $('[data-product=classic_walk_r] .card__image').attr("src", imagePathPNG);
});

$('[data-product="classic_run_r"] .color-icon').on("mouseover tap", function(event) {
  event.preventDefault();
  var sku = $(this).data("color");
  var imagePathPNG = $(this).data("image-path-png");
  var imagePathWEBP = $(this).data("image-path-webp");

  $('[data-product=classic_run_r] .card__image-container [type="image/png"]').attr("srcset", imagePathPNG);
  $('[data-product=classic_run_r] .card__image-container [type="image/webp"]').attr("srcset", imagePathWEBP);
  $('[data-product=classic_run_r] .card__image').attr("src", imagePathPNG);
});

$('[data-product="neo_run"] .color-icon').on("mouseover tap", function(event) {
  event.preventDefault();
  var sku = $(this).data("color");
  var imagePathPNG = $(this).data("image-path-png");
  var imagePathWEBP = $(this).data("image-path-webp");

  $('[data-product=neo_run] .card__image-container [type="image/png"]').attr("srcset", imagePathPNG);
  $('[data-product=neo_run] .card__image-container [type="image/webp"]').attr("srcset", imagePathWEBP);
  $('[data-product=neo_run] .card__image').attr("src", imagePathPNG);
});

$('[data-product="neo_walk"] .color-icon').on("mouseover tap", function(event) {
  event.preventDefault();
  var sku = $(this).data("color");
  var imagePathPNG = $(this).data("image-path-png");
  var imagePathWEBP = $(this).data("image-path-webp");

  $('[data-product=neo_walk] .card__image-container [type="image/png"]').attr("srcset", imagePathPNG);
  $('[data-product=neo_walk] .card__image-container [type="image/webp"]').attr("srcset", imagePathWEBP);
  $('[data-product=neo_walk] .card__image').attr("src", imagePathPNG);
});

$('[data-product="neo_walk_sondermodell"] .color-icon').on("mouseover tap", function(event) {
  event.preventDefault();
  var sku = $(this).data("color");
  var imagePathPNG = $(this).data("image-path-png");
  var imagePathWEBP = $(this).data("image-path-webp");

  $('[data-product=neo_walk_sondermodell] .card__image-container [type="image/png"]').attr("srcset", imagePathPNG);
  $('[data-product=neo_walk_sondermodell] .card__image-container [type="image/webp"]').attr("srcset", imagePathWEBP);
  $('[data-product=neo_walk_sondermodell] .card__image').attr("src", imagePathPNG);
});

$('[data-product="feetality"] .color-icon').on("mouseover tap", function(event) {
  event.preventDefault();
  var sku = $(this).data("color");
  var imagePathPNG = $(this).data("image-path-png");
  var imagePathWEBP = $(this).data("image-path-webp");

  $('[data-product=feetality] .card__image-container [type="image/png"]').attr("srcset", imagePathPNG);
  $('[data-product=feetality] .card__image-container [type="image/webp"]').attr("srcset", imagePathWEBP);
  $('[data-product=feetality] .card__image').attr("src", imagePathPNG);
});

$('[data-product="integer_walk_cushion"] .color-icon').on("mouseover tap", function(event) {
  event.preventDefault();
  var sku = $(this).data("color");
  var imagePathPNG = $(this).data("image-path-png");
  var imagePathWEBP = $(this).data("image-path-webp");

  $('[data-product=integer_walk_cushion] .card__image-container [type="image/png"]').attr("srcset", imagePathPNG);
  $('[data-product=integer_walk_cushion] .card__image-container [type="image/webp"]').attr("srcset", imagePathWEBP);
  $('[data-product=integer_walk_cushion] .card__image').attr("src", imagePathPNG);
});

$('[data-product="integer_run_rebound"] .color-icon').on("mouseover tap", function(event) {
  event.preventDefault();
  var sku = $(this).data("color");
  var imagePathPNG = $(this).data("image-path-png");
  var imagePathWEBP = $(this).data("image-path-webp");

  $('[data-product=integer_run_rebound] .card__image-container [type="image/png"]').attr("srcset", imagePathPNG);
  $('[data-product=integer_run_rebound] .card__image-container [type="image/webp"]').attr("srcset", imagePathWEBP);
  $('[data-product=integer_run_rebound] .card__image').attr("src", imagePathPNG);
});

$('[data-product="integer_walk_rebound"] .color-icon').on("mouseover tap", function(event) {
  event.preventDefault();
  var sku = $(this).data("color");
  var imagePathPNG = $(this).data("image-path-png");
  var imagePathWEBP = $(this).data("image-path-webp");

  $('[data-product=integer_walk_rebound] .card__image-container [type="image/png"]').attr("srcset", imagePathPNG);
  $('[data-product=integer_walk_rebound] .card__image-container [type="image/webp"]').attr("srcset", imagePathWEBP);
  $('[data-product=integer_walk_rebound] .card__image').attr("src", imagePathPNG);
});

$('[data-product="integer_walk_rebound_leder"] .color-icon').on("mouseover tap", function(event) {
  event.preventDefault();
  var sku = $(this).data("color");
  var imagePathPNG = $(this).data("image-path-png");
  var imagePathWEBP = $(this).data("image-path-webp");

  $('[data-product=integer_walk_rebound_leder] .card__image-container [type="image/png"]').attr("srcset", imagePathPNG);
  $('[data-product=integer_walk_rebound_leder] .card__image-container [type="image/webp"]').attr("srcset", imagePathWEBP);
  $('[data-product=integer_walk_rebound_leder] .card__image').attr("src", imagePathPNG);
});

$('[data-product="derby_walk_s"] .color-icon').on("mouseover tap", function(event) {
  event.preventDefault();
  var sku = $(this).data("color");
  var imagePathPNG = $(this).data("image-path-png");
  var imagePathWEBP = $(this).data("image-path-webp");

  $('[data-product=derby_walk_s] .card__image-container [type="image/png"]').attr("srcset", imagePathPNG);
  $('[data-product=derby_walk_s] .card__image-container [type="image/webp"]').attr("srcset", imagePathWEBP);
  $('[data-product=derby_walk_s] .card__image').attr("src", imagePathPNG);
});

$('[data-product="derby_run_s"] .color-icon').on("mouseover tap", function(event) {
  event.preventDefault();
  var sku = $(this).data("color");
  var imagePathPNG = $(this).data("image-path-png");
  var imagePathWEBP = $(this).data("image-path-webp");

  $('[data-product=derby_run_s] .card__image-container [type="image/png"]').attr("srcset", imagePathPNG);
  $('[data-product=derby_run_s] .card__image-container [type="image/webp"]').attr("srcset", imagePathWEBP);
  $('[data-product=derby_run_s] .card__image').attr("src", imagePathPNG);
});